@model IEnumerable<Aula1206_MVC_2.Models.Categoria>

    ViewBag.Title = "Categorias";
}

<h2>@ViewBag.Title</h2>

@*@foreach(string cat in ViewBag.ListaCategorias)
    {
      <!-- <p>@cat</p> - mostra a lista-->
      <!--<a href="#">@cat</a> - mostra o link -->
      //@ chama C#
        @Html.ActionLink(@cat,"Index","Categorias")
        @:|

    }*@

@foreach (var cat in Model)
{
    <dl>
        <dt>@cat.Nome</dt>
        @if (!string.IsNullOrEmpty(cat.Descricao))
        {
            <dt>@cat.Descricao</dt>
        }
        else
        {
            <dd>
                <em>Sem descrição</em>
            </dd>
        }


    </dl>

}

